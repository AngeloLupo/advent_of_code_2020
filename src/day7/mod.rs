use std::fs;

pub fn one() -> usize {
    let input = fs::read_to_string("src/day7/input").expect("Unable to read file");
    let smt: Vec<&str> = input.split("\n\n").collect();

    return 0;
}

pub fn two() -> usize {
    let input = fs::read_to_string("src/day7/input").expect("Unable to read file");
    let smt: Vec<&str> = input.split("\n\n").collect();

    return 0;
}